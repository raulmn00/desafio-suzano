VITE_API_URL=http://localhost:8080

# Sentry (error tracking do SPA). Sem VITE_SENTRY_DSN o Sentry fica DESATIVADO
# (o Error Boundary continua funcionando, só não reporta). Em produção, defina
# nas env vars do projeto na Vercel.
VITE_SENTRY_DSN=
VITE_SENTRY_TRACES_SAMPLE_RATE=0
